# repositories/testing/signatures/

Generated. Detached `<name>-<version>.mpkg.sig` files — written by `mitos-sign-package`. See docs/repository-format.md#signatures.

Nothing under this directory is hand-edited — see `docs/repository-format.md` for the full layout and `CONTRIBUTING.md` for how content actually gets here (via a PR to `packages/`, not a PR to this directory directly).
