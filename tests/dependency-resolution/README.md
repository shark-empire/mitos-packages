# tests/dependency-resolution/

Empty for now — see the "`dependency-resolution/`" section of
[`../README.md`](../README.md) for why this is empty on purpose rather
than an oversight (short version: resolving a dependency graph is
`mitos-pkg`'s job, not this repository's; this directory is reserved for
scenario fixtures a future cross-repository integration test, or a
maintainer's manual sanity check, would use).
